# The Pale Blue Dot — 전면 진단 및 개선 방향 보고서

**대상**: `palebluedot/` 전체 (HTML 9종, CSS 1, JS 1, 이미지 23)
**분석 범위**: 소스 코드 전량(5,505줄) 정독 + 이미지 자산 대조
**작성일**: 2026-08-02

---

## 0. 총평 — 한 문장 요약

> **"잘 만든 과학 전시관이지, 아직 실험실은 아니다."**

시각 완성도와 교과 내용의 밀도는 교사 개인 제작물 기준으로 상위권입니다. 다크 테마, Pretendard, 3단 레이아웃, Canvas 애니메이션까지 일관된 디자인 언어를 갖췄고, 8개 활동 모두 '조작 → 관찰 → 고찰 문제 → 원리 요약'이라는 공통 서사를 유지합니다. 이 정도의 구조적 일관성을 혼자 유지한 것은 분명한 성취입니다.

문제는 그 다음입니다. **이 사이트는 학생이 "보는" 자료이지 "탐구하는" 도구가 아닙니다.** 그리고 이는 선생님이 설정하신 프로젝트 목표(마이크로비트·스마트폰 센서 기반 능동적 과학탐구)와 정면으로 어긋납니다.

| 평가 축 | 점수 | 근거 |
|---|:---:|---|
| 시각 디자인·일관성 | **A−** | 통일된 디자인 토큰, 높은 완성도. 반응형 부재가 감점 |
| 인터페이스 설계 | **C+** | 3단 레이아웃은 좋으나 고정 폭·상태 저장 부재·피드백 빈약 |
| 과학적 정확성 | **C** | 치명적 오개념 3건, 명백한 오류 6건 이상 (§5) |
| 활동의 탐구성 | **D+** | 8개 중 6개가 '클릭 후 읽기'. 변인 통제·데이터 수집·결론 도출 없음 |
| 프로젝트 목표 정합성 | **D** | 센서 연동 0건. 사이트 소개문의 "Streamlit 백엔드"도 실체 없음 |
| 코드 건전성 | **C** | 동작은 하나 splice-in-forEach 등 전형적 결함 다수, 인라인 스타일 난립 |

---

## 1. 가장 먼저 마주해야 할 세 가지 구조적 문제

세부 버그보다 이 셋이 먼저입니다. 아래 문제를 두고 개별 파일을 다듬으면, 다듬을수록 잘못된 방향으로 정교해집니다.

### 1-1. 프로젝트의 정체성과 산출물이 어긋나 있습니다

선생님의 프로젝트 정의는 "학생이 **직접 센서 데이터를 수집·분석**하는 탐구 중심 수업 모델"입니다. 그런데 완성된 8개 활동 중 **학생이 실제 데이터를 생성하는 활동은 0개**입니다. 전부 개발자가 미리 짜 넣은 시나리오를 재생합니다.

가장 뼈아픈 지점은 `cloud_experiment.html`입니다. 페트병 단열팽창은 **실제로 교실에서 할 수 있는 실험**이고, 스마트폰 기압 센서(대부분의 안드로이드 기기 탑재)로 압력 변화를 실측할 수 있으며, 마이크로비트 온도 센서를 병 안에 넣어 온도 하강을 기록할 수 있습니다. 그런데 현재 구현은 **실물 실험을 마우스 클릭으로 대체**해 버렸습니다. 실물이 더 쉬운 것을 시뮬레이션으로 만든 셈입니다.

시뮬레이션이 정당화되는 조건은 명확합니다 — **직접 관찰이 불가능하거나(핵융합, 별의 진화, 지질시대), 시간·공간 스케일이 교실을 벗어날 때(탄소순환, 대멸종, 저기압 통과)**. 이 기준으로 8개를 재분류하면:

| 활동 | 시뮬레이션 정당성 | 판정 |
|---|---|---|
| 별의 핵융합 | 관찰 불가능 | ✅ 적절 |
| 별의 진화 | 시간 스케일 10⁹년 | ✅ 적절 |
| 지질 시대 | 시간 스케일 | ✅ 적절 |
| 생명체 대멸종 | 시간 스케일 | ✅ 적절 |
| 탄소 순환 | 공간·시간 스케일 | ✅ 적절 |
| 지구시스템 상호작용 | 개념 정리용 | △ 시뮬레이션일 필요 없음(인포그래픽으로 충분) |
| 중위도 저기압 | 관측 가능(기상청 실황 데이터) | ⚠️ 실데이터로 대체 가능 |
| **구름 발생 실험** | **교실에서 직접 가능** | ❌ **시뮬레이션이 실험을 밀어냈음** |

### 1-2. 학생의 산출물이 어디에도 남지 않습니다

8개 활동 전부에서 **학생이 입력한 것은 새로고침하면 사라집니다.** 우측 사이드바의 '생각해보기' 문항은 텍스트로만 존재하고 답을 쓸 입력란이 없습니다. 객관식이 있는 활동(핵융합, 별의 진화, 지질시대, 대멸종)도 정오만 표시할 뿐 기록되지 않습니다.

교사 입장에서 이는 곧: **평가할 근거가 없고, 학생 입장에서는 성실히 해도 남는 게 없다**는 뜻입니다. 정규 수업 40~50분에 투입하려면 반드시 해결해야 할 지점입니다. (해결책은 §7-2)

### 1-3. 학교 기기에서 화면이 깨집니다

모든 활동이 **고정 폭 3단 레이아웃 + 고정 크기 Canvas**입니다. 필요한 최소 가로폭을 계산하면:

| 파일 | 좌 사이드바 | Canvas | 우 사이드바 | **최소 폭** |
|---|---:|---:|---:|---:|
| cloud_experiment | 300 | 700 | 320 | **1,320px** |
| extratropical_cyclone | 310 | 700 | 320 | **1,330px** |
| earthsystem | 390 | 740 | 280 | **1,410px** |
| carboncycle | 390 | 740 | 270 | **1,400px** |
| nucleon_binding | 350 | 720 | 360 | **1,430px** |
| evolution_star | 340 | 720 | 340 | **1,400px** |

여기에 `body { overflow: hidden }`이 걸려 있어 **폭이 부족하면 스크롤도 안 되고 그냥 잘립니다.** 학교 보급형 노트북(1366×768)과 크롬북(1366×768)에서 우측 '생각해보기' 사이드바가 통째로 사라지거나, 세로 768px에서 하단 '핵심 원리 요약'(100~160px)이 잘립니다. 태블릿·스마트폰은 사용 불가입니다.

`nucleon_binding.html`은 세로도 위험합니다 — 헤더(≈54px) + `calc(100vh - 220px)` + 요약바(160px)로 계산이 어긋나 768px 화면에서 하단이 겹칩니다.

---

## 2. 공용 레이어 진단 (`index.html` / `style.css` / `script.js`)

### 잘된 점
- 과목별 드롭다운 분류가 교육과정 체계와 맞아떨어집니다.
- iframe SPA 방식은 활동 간 이동 시 깜빡임이 없고 구조가 단순해 유지보수에 유리합니다.
- 홈 화면의 NASA EPIC GIF + 칼 세이건 인용은 사이트 정체성을 잘 전달합니다.

### 문제점

**(a) 메뉴 11개 중 5개가 존재하지 않는 페이지입니다.**
`index.html` L38, L45, L52, L59, L66 — 모두 `https://example.streamlit.app/?embedded=true`. 실존하지 않는 예시 URL입니다. 학생이 클릭하면 빈 iframe 또는 오류 화면을 보게 됩니다.

특히 뼈아픈 것은 **선생님의 전공 과목인 '지구과학' 메뉴에 실제 콘텐츠가 0개**라는 점입니다. 완성된 8개는 전부 중3 과학·통합과학입니다.

> **즉시 조치**: 미구현 메뉴는 삭제하거나 `disabled` 처리 + "준비 중" 배지. 존재하지 않는 링크가 노출된 채로는 공개 수업에 못 씁니다.

**(b) 홈 화면이 외부 자산에 의존합니다.**
`style.css` L49의 NASA GIF는 외부 도메인 직링크입니다. 학교망 방화벽·NASA 측 경로 변경 시 배경이 통째로 사라집니다. `@import`로 불러오는 Pretendard(jsDelivr)도 동일한 위험이 있고, 차단 시 전체 폰트가 시스템 기본체로 떨어집니다. **자산 로컬화가 필요합니다.**

**(c) `script.js` L22의 캐싱 로직이 작동하지 않습니다.**
```js
if (contentFrame.src !== targetUrl) { contentFrame.src = targetUrl; }
```
`iframe.src`는 **절대 URL**(`file:///.../cloud_experiment.html`)을 반환하고 `targetUrl`은 **상대 경로**(`cloud_experiment.html`)입니다. 두 값은 영원히 다르므로 같은 메뉴를 다시 눌러도 매번 리로드됩니다. 학생이 실수로 같은 메뉴를 재클릭하면 진행 중이던 시뮬레이션이 초기화됩니다.

**(d) 뒤로 가기·딥링크가 없습니다.**
브라우저 뒤로 가기가 작동하지 않고(URL이 안 바뀜), 특정 활동으로 바로 가는 링크를 학생에게 배포할 수 없습니다. 수업에서 "3번 활동 열어라"를 매번 구두로 안내해야 합니다. → `location.hash` 라우팅으로 해결 (§8-1)

**(e) 접근성 전무.**
`<button>`에 `aria-*` 없음, 드롭다운이 `:hover` 전용이라 키보드 Tab 이동으로 열 수 없음, 색상 대비 미검증. 특수교육 대상 학생이 있는 학급에서는 문제가 됩니다.

---

## 3. 활동별 정밀 분석

### 3-1. `cloud_experiment.html` — 중3 구름 발생 실험

**인터페이스** — 8개 중 조작 설계가 가장 좋습니다. "페트병을 연속 클릭해 가압 → 뚜껑 클릭해 개방"은 실물 실험의 신체 동작을 화면으로 옮긴 것이라 학생이 설명 없이 이해합니다. 압력계 바늘, 실시간 온도/압력 수치, 상태 텍스트의 3중 피드백도 적절합니다.

**디자인** — 페트병 반투명 렌더링, 충돌 시 입자가 주황색으로 점멸하는 처리(L556, `collisionTimer`)는 "압축 = 충돌 빈도 증가"를 시각적으로 잘 전달합니다.

**과학적 문제**

1. **온도 변화가 물리적으로 틀렸습니다.** L461:
   ```js
   temperature -= (finalPressure * 7.5);
   ```
   단열 과정의 온도 변화는 **압력의 절대값이 아니라 압력비**에 지배됩니다. 포아송 관계 `T₂/T₁ = (P₂/P₁)^((γ−1)/γ)`, 공기 γ=1.4일 때 지수는 0.286입니다. 현재 코드는 `1.0 atm`에서 열어도 `−7.5°C`가 적용됩니다. 실제로는 압력비 1.0 → 온도 변화 0이어야 합니다.

   **올바른 식**:
   ```js
   const T1 = temperature + 273.15;
   const T2 = T1 * Math.pow(1.0 / finalPressure, 0.286);
   temperature = T2 - 273.15;
   ```
   2.3 atm → 1.0 atm이면 293K → 233K, 즉 20°C → **−40°C**. 지나치게 큽니다. 실제 페트병 실험이 −2~−5°C인 이유는 완전 단열이 아니고 팽창이 순간적이지 않기 때문 — 여기에 효율 계수 0.1을 곱하면 실측과 맞아떨어지고, **"이론값과 실측값의 차이는 왜 생길까?"라는 최고의 탐구 문항**이 생깁니다.

2. **압축 시 온도 상승도 임의값입니다.** L440 `temperature += 1.2`(클릭당 고정). 같은 포아송 관계를 쓰면 압축·팽창이 하나의 식으로 통일되고, 학생이 "가열이 아니라 압축이 온도를 올린다"를 정량으로 확인합니다.

3. **이슬점 개념이 빠져 있습니다.** 하단 요약문에는 "온도가 이슬점 아래로 떨어지면"이라고 적어 놓고, 정작 시뮬레이션은 **이슬점을 계산하지 않습니다.** `cloudDensity = (humidity/100) * 0.85`(L463) — 습도에 선형 비례할 뿐, 온도와 무관하게 항상 구름이 생깁니다. 압력을 조금만 올리고 열어도 구름이 나옵니다.

   > **이것이 이 활동의 가장 심각한 문제입니다.** 중3 성취기준의 핵심은 "**단열 팽창 → 온도 하강 → 이슬점 도달 → 응결**"의 인과 사슬인데, 세 번째 고리가 통째로 빠져 학생이 "습도가 높으면 구름이 생긴다"는 축약된 오개념을 얻습니다.

4. `humiditySlider` 리스너(L407)가 `pressure === 1.0`일 때만 동작합니다. 부동소수점 누적(`+= 0.08`)으로 정확히 1.0이 아닌 경우가 생기고, 무엇보다 **한 번 실험한 뒤 습도만 바꿔 재실험하려면 리셋을 눌러야 한다는 안내가 없습니다.** Q3가 "10%와 100%를 각각 설정해 실험하라"고 요구하는데, 학생은 슬라이더가 안 움직이는 이유를 모릅니다.

**교육 효과 판정** — 개념 전달은 되지만 **탐구가 아닙니다.** 변인(습도)이 하나뿐이고 그 결과도 정성적("진하다/연하다")이라 데이터를 남길 수 없습니다.

**개선 방향**
- **[최우선] 실물 실험으로 되돌리고, 이 페이지는 '데이터 기록·분석 도구'로 전환하세요.** 페트병 + 스마트폰 기압 센서(`phyphox` 앱 또는 Web `Barometer` API) + 마이크로비트 온도 센서로 실측하고, 이 페이지는 실측값을 입력받아 P–T 그래프를 그리고 포아송 곡선과 겹쳐 보여 주는 역할을 맡습니다. **시뮬레이션이 아니라 계측 대시보드**가 되는 것이 프로젝트 목표와 맞습니다.
- 시뮬레이션을 유지한다면: 이슬점(Magnus 식) 도입 → 온도가 이슬점 아래로 내려간 순간에만 응결 발생 → **"습도 30%일 때 구름을 만들려면 최소 몇 atm까지 압축해야 하는가?"** 형태의 정량 미션으로 전환. 이러면 슬라이더가 진짜 변인이 됩니다.

---

### 3-2. `extratropical_cyclone.html` — 중3 중위도 저기압

**인터페이스** — **8개 중 정보 설계가 가장 뛰어납니다.** 상단 평면 일기도 + 하단 연직 단면 + 최하단 시계열 그래프의 3단 동기화는 중위도 저기압 단원의 고질적 난점(평면과 연직을 머릿속에서 합성하기)을 정면으로 해결합니다. 위도 슬라이더로 "저기압 중심의 남쪽/북쪽을 통과할 때 날씨가 어떻게 다른가"를 비교하게 만든 설계도 교과서보다 낫습니다. **이 파일은 사이트의 대표작으로 밀 만합니다.**

**디자인** — 표준 전선 기호(빨간 반원/파란 삼각형)를 각도까지 맞춰 그린 점(L473~487), 층운형은 넓고 얇은 타원, 적운형은 수직 발달 그라디언트로 구분한 점 모두 정확합니다.

**과학적 문제**

1. 🔴 **[치명] 저기압 북쪽 통과 시 풍향이 전부 틀렸습니다.** L382~384 / L606~608.

   북반구 저기압은 반시계 방향으로 수렴합니다. 관측소가 중심의 **북쪽**에 있을 때, 위치별 풍향을 벡터로 계산하면:

   | 관측소 위치 | 코드 표시 | **실제** |
   |---|---|---|
   | 중심의 서쪽 (`distX < -40`) | 동풍(E) | **북동풍(NE)** |
   | 중심의 바로 북쪽 (`|distX| ≤ 40`) | 북풍(N) | **동풍(E)** |
   | 중심의 동쪽 (`distX > 40`) | 북서풍(NW) | **남동풍(SE)** |

   세 경우 모두 오답이며, 풍향 화살표의 회전각도 함께 틀려 있습니다. 코드 주석에는 `// [고증 완료] ... 벡터 매핑 교정`이라 적혀 있으나 교정되지 않았습니다.

   **수정 코드**:
   ```js
   // updateWeatherInfo() 내 else 블록
   if (distX < -40)              windText.innerText = "북동풍 (NE)";
   else if (Math.abs(distX) <= 40) windText.innerText = "동풍 (E)";
   else                            windText.innerText = "남동풍 (SE)";

   // drawWindCompass() 내 else 블록 (화살표는 '바람이 부는 쪽'을 가리킴)
   if (distX < -40)               targetAngle =  Math.PI * 0.75;  // → 남서
   else if (Math.abs(distX) <= 40) targetAngle =  Math.PI;         // → 서
   else                            targetAngle = -Math.PI * 0.75;  // → 북서
   ```

2. 🔴 **상태판 기온과 그래프 기온이 서로 다릅니다.** 같은 순간, 같은 관측소인데:

   | 구역 | 상태판(L365~374) | 그래프(L398~401) |
   |---|---:|---:|
   | 온난전선 전면 | 15°C | **11°C** |
   | 온난역 | 22°C | 22°C |
   | 한랭전선 직후 | 18°C | **13°C** |
   | 한랭전선 후면 | 12°C | **6°C** |

   Q2가 "**시뮬레이터 계측기 수치를 기반으로** 기록해 보세요"라고 지시하므로, 학생은 두 값 중 무엇을 적을지 알 수 없습니다. **단일 소스로 통합**해야 합니다(권장: 그래프 값을 기준으로 상태판을 맞춤).

3. **한랭전선 통과 시 기온이 '먼저 오르는' 현상.** 상태판 순서가 22 → 18 → 12인데, 이는 온난역에서 한랭전선 통과 후 **계단식 하강**입니다. 실제 한랭전선 통과는 **급락**이고 이것이 온난전선(완만한 상승)과의 결정적 대비점입니다. 그래프에서도 13 → 6이라 '급락'의 인상이 약합니다. 통과 직후 값을 온난역과 동일하게 두었다가 전선 통과 순간 급락시키는 편이 개념 전달에 유리합니다.

4. **풍향 화살표에 범례가 없습니다.** 기상학 관례에서 풍향은 "바람이 **불어오는** 방향"인데, 이 화살표는 "**불어가는** 방향"을 가리킵니다(코드상 일관적입니다). 학생이 "남동풍인데 왜 화살표가 북서쪽을 향하지?"에서 반드시 걸립니다. 캔버스에 `↑ 화살표는 공기가 이동하는 방향` 한 줄만 추가하면 해결됩니다.

5. 위도 슬라이더 초기 라벨이 `북쪽 통과`(L270)인데 초기값 100은 "관측자가 중심의 **남쪽**"에 해당합니다. 최초 진입 시 라벨과 상태가 불일치합니다(첫 조작 후 정정됨).

**교육 효과 판정** — 잠재력 최상. 다만 위 1·2번을 고치지 않으면 **오개념을 정확하게 전달하는 도구**가 됩니다.

**개선 방향**
- 1·2번 즉시 수정.
- **[확장] 기상청 실황 데이터 연결.** 이 활동은 실데이터 접목이 가장 쉽습니다. 기상청 API 허브(`apihub.kma.go.kr`)의 지상 관측 자료로 실제 저기압 통과일(예: 봄철 황사 저기압)의 서울·대전·부산 기온/기압/풍향 시계열을 받아 오면, 학생이 **"이 날 전선은 언제 통과했는가?"를 실제 데이터로 역추적**할 수 있습니다. 시뮬레이션에서 익힌 패턴을 실데이터에 적용하는 2단 구성 — 이것이 진짜 탐구입니다.
- 학생이 관측소를 캔버스 위에 **직접 배치**할 수 있게 하면(현재 고정, `observerFlat`), 위도 슬라이더보다 훨씬 직관적이고 "우리 학교 위치라면?" 같은 질문이 열립니다.

---

### 3-3. `nucleon_binding.html` — 통합과학 별의 핵융합(p-p 반응)

**인터페이스** — 온도 슬라이더 하나로 임계점(1,000만 K) 돌파를 체험시키는 단순함이 장점입니다. 좌측 단계 가이드(1→2→3단계 도식)와 실시간 상태 텍스트의 연동도 좋습니다.

**과학적·기술적 문제**

1. 🔴 **[치명] 중수소가 화면에서 사라집니다.** L494에서 1단계 성공 시 `p1.type = 'neu'`로 바꾸는데, `Particle.draw()`(L371~387)는 `'pro' | 'deu' | 'he3' | 'he4'`만 처리합니다. **`'neu'` 타입에는 그리기 분기가 없어 아무것도 렌더링되지 않습니다.** 학생 눈에는 "양성자 두 개가 충돌하더니 **둘 다 사라졌다**"로 보입니다. 핵융합 = 소멸이라는 정반대 오개념을 심습니다.

   덤으로 정의만 되고 한 번도 쓰이지 않는 `'deu'` 타입이 남아 있고, `typeRadius('neu')`는 기본값 9를 반환해 충돌 판정 크기도 틀립니다.

   **수정**:
   ```js
   // draw() 에 추가
   } else if (this.type === 'neu') {
       drawCluster(this.x, this.y, 1, 1);   // 양성자1 + 중성자1 = 중수소
   }
   // typeRadius() 수정
   if (type === 'pro') return 9;
   if (type === 'neu' || type === 'deu') return 12;
   ```
   `drawCluster`의 주석도 틀려 있습니다 — L380 `'deu'`에 "양성자 2, 중성자 1"이라 적혀 있으나 인자는 `(1,1)`입니다.

2. **충돌 처리가 물리적으로 틀렸습니다.** L480~481:
   ```js
   p1.vx *= -1; p1.vy *= -1;
   p2.vx *= -1; p2.vy *= -1;
   ```
   두 입자의 속도를 모두 반전시키는 것은 탄성 충돌이 아니며 운동량 보존을 위배합니다. 나란히 가던 두 입자가 스치면 **둘 다 뒤로 튕기는** 기이한 장면이 나옵니다. 법선 방향 속도만 교환해야 합니다.

3. **`checkCollisions()`가 프레임당 한 번의 융합만 처리하고 `return`합니다**(L476). 입자 150개 규모에서는 반응 속도가 비현실적으로 느려지고, `O(n²)`(11,175회/프레임) 순회 후 대부분을 버립니다.

4. **하이라이트 버튼이 시뮬레이션을 파괴합니다.** L520에서 `particles` 배열을 헬륨-3 두 개로 **통째로 교체**합니다. 결과적으로 좌측 "실시간 성분 질량비"가 즉시 **수소 0% / 헬륨 100%**로 튀어 "별이 순식간에 헬륨이 됐다"는 오개념을 만듭니다. 기존 입자를 유지한 채 두 개만 추가·가속해야 합니다.

5. **통합과학 성취기준의 핵심인 질량 결손이 정량화되어 있지 않습니다.** 하단 요약 카드에 `E=mc²`가 문장으로만 있고, 시뮬레이션 어디에도 수치가 없습니다. 이 단원의 성취기준은 "**수소 원자핵 4개의 질량 > 헬륨 원자핵 1개의 질량**임을 알고 그 차이가 에너지로 전환됨을 설명"입니다.

   ```
   4 × ¹H  =  4 × 1.00728 u  =  4.02912 u
   ⁴He     =              4.00260 u
   Δm      =              0.02652 u   (0.66%)
   E = Δm·c² = 24.7 MeV = 3.95 × 10⁻¹² J
   ```
   **헬륨-4가 하나 생성될 때마다 이 수치가 누적 표시되는 카운터**를 넣으면 활동의 격이 달라집니다.

6. **임계온도가 계단 함수입니다**(L475, `temperature >= 1000`). 999만 K에서는 아무 일도 없다가 1,000만 K에서 갑자기 시작합니다. 실제로는 맥스웰 분포 꼬리 + 양자 터널링으로 **확률적**이며, "온도가 높을수록 반응률이 급격히 증가"가 옳습니다. 확률을 온도의 연속 함수로 두면(예: `0.18 * Math.max(0, (T-800)/1200)`) "임계점"의 의미가 오히려 선명해집니다.

7. HTML 구조 오류: `<body>` 태그가 두 번 열립니다(L216, L218). `<style>`의 클래스 정의와 인라인 `style` 속성이 같은 요소에 중복 적용되어(예: `.summary-section` 높이 150px vs 인라인 160px) 유지보수가 어렵습니다.

8. 오답 선택 시 **왜 틀렸는지 설명이 없습니다.** 빨간 테두리만 표시됩니다. 정답 해설만 있고 오답 해설이 없는 구조는 형성평가로서 절반의 기능만 합니다. (같은 사이트의 `exodus.html`은 오답별 해설을 갖추고 있습니다 — **내부에 이미 좋은 사례가 있으니 그 패턴을 이식하면 됩니다.**)

**교육 효과 판정** — 1번 버그가 살아 있는 한 **역효과**입니다. 수정 후에는 양호.

---

### 3-4. `evolution_star.html` — 통합과학 별의 진화

**인터페이스** — 질량 2택 → 4단계 타임라인 → 물리량 게이지의 흐름이 명료합니다. 좌(외형) / 우(내부 구조) 2분할 캔버스로 "겉모습"과 "속모습"을 동시에 보여 주는 발상이 좋습니다. 초거성 양파 구조의 색상 레이어링도 효과적입니다.

**문제점**

1. 🔴 **H-R도가 없습니다.** 별의 진화 단원에서 학습해야 할 것은 **H-R도 위의 이동 경로**입니다. 이 활동은 표면온도와 반지름을 "막대 그래프"로 보여 주는데, 이 두 값은 **H-R도의 두 축 그 자체**입니다. 데이터는 이미 다 있고 축만 바꾸면 됩니다.

   > **가장 비용 대비 효과가 큰 개선**: 게이지 3개를 H-R도 산점도로 교체하고, 단계 클릭 시 별의 위치가 진화 트랙을 따라 이동하도록 하기. 저질량 트랙(주계열 → 우상향 적색거성 → 좌하단 백색왜성)과 고질량 트랙(좌상단 → 우측 초거성)을 겹쳐 그리면 **"질량이 진화 경로를 결정한다"**는 핵심이 한 화면에 담깁니다.

2. **초신성 단계의 물리량이 사실이 아닙니다.** L182: `r: 5000 R☉, ts: 500000 K, tc: 1000억 K`. 초신성은 **팽창하는 폭발 잔해**이므로 "반지름 5,000 R☉", "표면 온도 50만 K" 같은 항성 물리량 자체가 정의되지 않습니다. 다른 세 단계는 실제 값에 가까운데(적색거성 120 R☉·1.2억 K, 백색왜성 0.01 R☉ 모두 타당), 이 항목만 임의값이라 **데이터의 신뢰성 전체가 흔들립니다.** "측정 불가 / 폭발 잔해"로 명시하는 편이 정직하고 교육적입니다.

3. **10 M☉ 별의 최종 산물이 빠졌습니다.** 4단계가 "초신성 폭발 & 잔해"에서 끝나고 **중성자별**이 언급되지 않습니다. 저질량 트랙은 백색왜성까지 명확히 도달하는데 고질량 트랙만 결말이 없어 대칭이 깨집니다. (10 M☉은 중성자별, 25 M☉ 이상이 블랙홀 — 질량 선택지에 30 M☉을 추가하면 세 갈래 비교가 완성됩니다.)

4. **Q2 해설의 설명이 부정확합니다.** L128 "표면 **확산**으로 인해 표면 온도는 낮아져". '확산'은 여기서 부적절한 용어입니다. 정확히는 *"같은 에너지를 훨씬 넓어진 표면적에서 방출하므로 단위 면적당 방출량(∝T⁴)이 줄어 표면 온도가 낮아진다"* — 슈테판·볼츠만 법칙과 연결되는 지점이라 정확히 쓸 가치가 있습니다.

5. `selectMass()`(L189)가 전역 `event` 객체에 의존합니다. Chrome에서는 동작하지만 표준이 아니며 Firefox/Safari에서 오류가 납니다. 학교 환경이 Chrome 일변도가 아니라면 위험합니다. → `onclick="selectMass('low', this)"`로 요소를 명시 전달.

6. 정적 화면인데 `requestAnimationFrame`으로 초당 60회 전체 재렌더링합니다(L334). 저사양 크롬북에서 불필요한 발열·배터리 소모를 유발합니다. 맥동 효과가 필요한 단계에서만 애니메이션을 돌리세요.

**교육 효과 판정** — 개념 나열은 충실하나 **학생이 할 일이 '버튼 4번 누르기'뿐**입니다. H-R도 도입 + "관측된 별의 표면온도·광도를 주고 진화 단계를 판정하기" 미션을 얹으면 탐구 활동이 됩니다.

---

### 3-5. `earthsystem.html` — 통합과학 지구시스템 상호작용

**인터페이스** — 4×4 매트릭스 패드는 **개념적으로 매우 훌륭한 설계**입니다. "원인(행) × 결과(열)"의 격자는 상호작용 16가지를 체계적으로 보여 주고, 학생이 빠뜨린 조합을 스스로 발견하게 합니다. 이 UI 아이디어는 사이트 전체에서 가장 독창적입니다.

**문제점**

1. 🔴 **텍스트에 태국어가 섞여 있습니다.** L305:
   > "무역풍**และ** 편서풍이 바닷물 표면과…"

   `และ`는 태국어로 '그리고'입니다. 복사·번역 과정의 오염으로 보입니다. **학생에게 그대로 노출되는 문장**이므로 즉시 수정해야 합니다.

2. **활동이 '클릭 → 읽기'에 머뭅니다.** 16개 패드 각각이 짧은 애니메이션과 설명문을 재생하고 끝납니다. 학생이 판단하거나 선택할 것이 없습니다. 우측 고찰 문항은 **16개 패드 중 단 2개**(기→지, 생→지)만 다룹니다.

   > **개선 방향 — 매트릭스를 '문제'로 뒤집기**: 지금은 정답(16칸이 이미 채워진 표)을 보여 줍니다. 대신 **빈 격자를 주고 "화산 폭발로 항공기가 결항되었다"** 같은 실제 사례 카드를 제시해 학생이 해당 칸에 끌어다 놓게 하면, 같은 UI가 그대로 **진단·평가 도구**가 됩니다. 코드 변경량 대비 교육적 전환이 가장 큰 개선입니다.
   > 실제 사례 소재: 2010 에이야퍄들라이외퀴들 화산 / 2011 동일본 지진 해일 / 매년 봄 황사 / 낙동강 녹조 / 새만금 간척.

3. **정확성 보완이 필요한 설명**
   - **해양 염류(G_H, L310)**: 나트륨·마그네슘 등 양이온이 암석 풍화에서 온다는 설명은 맞으나, **염화 이온(Cl⁻)은 화산 가스 탈기체에서 유래**한다는 점이 빠져 "바닷물의 소금은 전부 육지에서 왔다"는 오개념을 남깁니다. 해수 염류 중 가장 많은 것이 Cl⁻이므로 한 문장 보완이 필요합니다.
   - **심층 순환(H_H, L315)**: "결빙 → 염분 증가 → 밀도 증가 → 침강"은 정확합니다. 다만 밀도를 결정하는 두 요인(수온·염분) 중 **수온의 기여가 더 크다**는 점이 빠져 있습니다.

4. **코드 결함**
   - L536 `stroke=ctx.stroke();`, L693 `strokeColor = ctx.strokeStyle;` — `var/let` 없는 **암묵적 전역 변수** 생성. `'use strict'` 하에서는 오류입니다.
   - L648 `particles.forEach((p, idx) => { ... particles.splice(idx,1) ... })` — **순회 중 배열 삭제**로 인덱스가 밀려 매 프레임 일부 입자가 갱신을 건너뜁니다. 화면이 미세하게 끊깁니다. → 역순 `for` 루프 또는 `filter` 사용.
   - `A_G`(버섯바위) 파티클이 `x: 45~105, y: 150~200` 상자 안에서 영원히 튕깁니다(L657). 풍화 산물이 **날아가 사라지지 않고 갇혀 있어** 시간이 지나면 주황색 덩어리가 됩니다.

---

### 3-6. `carboncycle.html` — 통합과학 탄소 순환

**인터페이스** — **다중 선택(중첩) 방식이 이 활동의 결정적 강점입니다.** 광합성과 호흡을 동시에 켜서 반대 방향 흐름을 겹쳐 보게 한 설계는 "동적 평형"을 직관적으로 전달합니다. 좌측 그룹 분류(기권↔생물권 / 기권↔수권 / 지권·인간활동)도 교과서 구조와 잘 맞습니다.

**문제점**

1. 🔴 **탄소 저장량이 어디에도 없습니다.** 우측 Q2는 *"실제 지구 시스템에서 대다수의 탄소는 …지권에 고착화되어 있습니다. 인간 활동이 이를 깨뜨리는 이유를 고찰하세요"*라고 묻는데, **화면에는 그 '대다수'를 보여 주는 수치가 하나도 없습니다.** 코드 주석(L112)에 "오개념 수치판 대신"이라 적혀 있어 의도적으로 뺀 것으로 보이나, 결과적으로 **질문이 근거 없이 떠 있습니다.**

   탄소 저장소 규모는 로그 스케일로 압도적이며, 이 격차 자체가 학습 목표입니다:

   | 저장소 | 규모 (Gt C) |
   |---|---:|
   | 지권(석회암·퇴적암) | ~100,000,000 |
   | 화석 연료 | ~4,000 |
   | 해양(용존) | ~38,000 |
   | 대기 | ~875 |
   | 육상 생물권·토양 | ~2,300 |

   각 권역 라벨 옆에 이 수치와 **로그 스케일 막대**를 붙이기만 해도 Q2가 답 가능한 질문이 됩니다. (선형 막대는 지권이 화면을 다 먹으므로 반드시 로그 스케일로, 그리고 "왜 로그로 그려야 하는가"가 또 하나의 좋은 질문이 됩니다.)

2. **빠른 순환과 느린 순환이 구분되지 않습니다.** 광합성(수 시간)과 화석연료 형성(수백만 년)이 **동일한 속도의 파티클**로 표현됩니다. "탄소는 순환하니까 괜찮다"는 흔한 오개념의 정확한 원인이 여기 있습니다. **파티클 속도를 실제 시간 스케일의 로그에 비례**시키면 — 화석연료 형성 입자가 거의 멈춰 있는 것처럼 보이면 — 개념이 시각적으로 각인됩니다.

3. **라벨 오류**: L476, '용해(기권→수권)' 파티클이 대기 중에서부터 `HCO₃⁻`로 표시됩니다. 대기 중에서는 `CO₂`이고, 해수에 녹은 **후에** `HCO₃⁻`로 전환됩니다. 파티클이 해수면(y=220)을 통과하는 시점에 라벨을 바꾸면 "용해 = 화학종 변화"까지 담을 수 있습니다.

4. 라벨이 `tick % 2 === 0` 조건으로만 그려져(L504) **초당 30회 점멸**합니다. 읽기 어렵고 눈이 피로합니다. 매 프레임 그리세요.

5. §3-5와 동일한 `splice`-in-`forEach` 결함(L497).

**교육 효과 판정** — 중첩 UI라는 좋은 뼈대에 **정량 데이터만 얹으면** 단숨에 최상위 활동이 됩니다. 개선 우선순위가 매우 높습니다.

---

### 3-7. `geoperiod.html` — 통합과학 지질 시대 (박물관 타이쿤)

**인터페이스** — 게임 메커니즘(자금·업그레이드·수집)으로 학습 동기를 끌어내려는 시도는 이해되며, 화석 상세 모달의 **'생전 복원도 ↔ 실제 화석 사진' 2단 비교는 매우 좋은 자료 설계**입니다. 화석을 처음 보는 학생이 "이 돌이 저 생물이었다"를 연결하는 데 결정적입니다. 16장의 이미지를 직접 수집·배치한 노력이 드러납니다.

**문제점**

1. 🔴🔴 **[가장 심각] 지층 누중의 법칙을 정반대로 가르칩니다.**

   시추 시나리오가 이렇게 흐릅니다(L717~725):
   - 1단계: **지표면 (0m)** → 선캄브리아대·고생대 발굴
   - 2단계: *"더 깊은"* **중생대 지층 (지하 500m)** 개방
   - 3단계: *"최종 관문"* **신생대 지층 (지하 100m)** 개방

   즉 **가장 오래된 선캄브리아대가 지표면에, 가장 젊은 신생대가 가장 깊은 곳**에 있는 구조이며, "기술이 발달해 더 깊이 판다 → 더 최근 시대가 나온다"는 서사입니다. **지질학의 제1원리인 지층 누중의 법칙과 완전히 반대**입니다.

   설상가상으로 3단계의 깊이 표기가 500m → **100m**로 오히려 얕아지면서, 텍스트("더 깊은")와 숫자가 서로 모순됩니다.

   > 이 활동의 성취기준이 바로 "**지층과 화석을 통해 지질시대를 구분한다**"인데, 게임 진행 자체가 그 원리를 위배합니다. 학생은 게임을 성실히 할수록 오개념을 강화합니다. **반드시 순서를 뒤집어야 합니다** — 신생대(얕음, 100m) → 중생대(500m) → 고생대(1,500m) → 선캄브리아대(3,000m).

2. 🔴 **지질시대의 상대적 길이가 왜곡됩니다.** 하단 타임라인(L396~401)이 선캄브리아대·고생대·중생대·신생대를 `justify-content: space-around`로 **균등 배치**합니다.

   실제 비율:

   | 시대 | 기간 | 비율 |
   |---|---:|---:|
   | 선캄브리아대 | 46억~5.4억 년 전 | **88.2%** |
   | 고생대 | 5.4억~2.5억 | 6.3% |
   | 중생대 | 2.5억~0.66억 | 4.0% |
   | 신생대 | 0.66억~현재 | **1.4%** |

   "지질시대의 대부분은 선캄브리아대"는 이 단원에서 학생이 가장 놀라고 가장 오래 기억하는 사실이자 명시적 성취기준입니다. 균등 배치는 이를 정면으로 지웁니다. **`flex-grow`를 비율대로 주기만 하면 되는 5분짜리 수정**이며, 효과는 큽니다.

3. **게임 목표와 학습 목표가 분리되어 있습니다.** 학생이 실제로 하는 행동은 ① 관람객이 떨군 코인에 마우스 올리기 ② 버스·자석 업그레이드 ③ 시추 버튼 누르기입니다. 이 중 **지질학적 판단이 필요한 행동은 하나도 없습니다.** 어떤 화석이 나올지는 `Math.random()`으로 결정되고(L734), 학생은 결과를 통보받을 뿐입니다.

   교육게임 설계에서 이를 '외재적 판타지(exogenous fantasy)'라 하며, 학습 내용을 게임 껍데기가 감싸기만 할 때 **학습 효과가 오히려 떨어진다**는 것이 알려져 있습니다. 학생은 지질시대가 아니라 코인 수집을 학습합니다.

   > **전환 방향**: 시추 시 **무작위 화석 대신 "지층 시료"를 제시**하고, 학생이 산출 화석·암상을 근거로 **어느 지질시대인지 판정**하게 하세요. 맞히면 전시, 틀리면 재분석. 이러면 코인·자금이 **판정 실력의 보상**이 되어 게임 목표와 학습 목표가 정렬됩니다. 게임 요소를 버릴 필요는 없고, 무엇에 보상하는지만 바꾸면 됩니다.

4. **퀴즈 설계 결함**
   - 5문항이 순환 반복되므로, 두 바퀴째부터 **정답을 외워 무한히 100G를 획득**할 수 있습니다(L830). 자금 밸런스가 무너지고, 무엇보다 학생이 개념 대신 답 위치를 외웁니다.
   - 오답 시 해설 없이 2초 뒤 다음 문제로 넘어가며(L829), **재시도가 불가능**합니다. 틀린 학생은 왜 틀렸는지 모른 채 지나갑니다.
   - 문항 자체의 질은 좋습니다(특히 "태백 석회암층 삼엽충 → 고환경 추론"). 문항 수를 20개 이상으로 늘리고 무작위 출제 + 오답 해설을 붙이면 그대로 형성평가로 쓸 수 있습니다.

5. **내용 오류**
   - L484 "중생대를 호령하던 거대 **거충류**들이" — 오타 또는 존재하지 않는 분류군입니다. '지배파충류(archosaur)' 또는 '공룡류'로 정정 필요.
   - L468 완족류: "**좌우 대칭이 아니라** 등껍질과 배껍질의 크기가 서로 다른" — 정확히는 완족류는 **각 껍데기가 좌우 대칭**이고 위·아래 껍데기의 크기가 다릅니다. 반대로 이매패류(조개)는 좌우 껍데기가 서로 거울상이지만 각 껍데기 자체는 비대칭입니다. 현재 문장은 완족류가 비대칭이라는 오해를 부릅니다.
   - **표준화석의 조건**(넓은 분포 × 짧은 생존 기간)이 어디에도 설명되지 않습니다. 화석 8종을 "표준화석"으로 부르면서 왜 표준화석인지를 다루지 않는 것은 큰 누락입니다.

6. **CSS 문법 오류**: L44 `.header class="stats-top" { ... }` — 유효한 선택자가 아니어서 이 블록은 통째로 무시됩니다(다행히 L50에 정상 규칙이 있어 시각적 영향은 없습니다).

7. `Visitor.draw()`의 `legOffset`(L577)이 계산만 되고 사용되지 않습니다. 걷기 애니메이션이 의도되었다가 누락된 것으로 보입니다.

---

### 3-8. `exodus.html` — 통합과학 5대 대멸종

**인터페이스** — **8개 중 학습 설계가 가장 성숙합니다.**
- 좌측 지층 주상도가 **위=최신, 아래=오래됨**으로 올바르게 배치되어 있습니다(`geoperiod.html`과 정반대 — 같은 주제인데 한쪽만 맞습니다).
- 이전 단계 퀴즈를 맞혀야 다음 지층이 열리는 **잠금 진행 구조**는 학생이 순서를 지켜 인과를 따라가게 만듭니다.
- **오답 선택지마다 개별 해설**을 준비한 것(`wrongExplains`)은 사이트 전체에서 유일하며, 형성평가로서 제대로 기능합니다. **이 패턴을 다른 활동에 그대로 이식하세요.**
- 안정기(고생대 초·중생대 전기)를 멸종 사이에 배치해 "**멸종은 예외적 사건**"이라는 맥락을 준 구성도 좋습니다.
- 4권역 인과 사슬 다이어그램은 "대멸종 = 지구시스템 연쇄 반응"이라는 통합과학 관점을 정확히 구현합니다.

**문제점**

1. 🔴 **완족류를 절멸했다고 표시합니다.** L771에서 1차 대멸종 시 완족류를 `deadState = true`로 처리해 캔버스에 **"[절멸 화석] 완족류 ❌"**가 뜹니다. 완족류는 오르도비스기 말에 큰 타격을 입었을 뿐 **현재도 생존하는 분류군**입니다. 우측 화석 카드에는 "큰 타격을 입었습니다"라고 정확히 적혀 있어 **화면과 설명이 모순**됩니다.

   필석도 마찬가지로 오르도비스기 말에 절멸 표시되지만, 실제로는 실루리아기를 거쳐 데본기 초까지 생존했습니다. **"타격(감소)"과 "절멸"의 구분**은 이 단원에서 반드시 유지되어야 하는 개념입니다.

2. 🔴 **4차 대멸종 화면에서 아무 일도 일어나지 않습니다.** L774는 `activeEra === 5`일 때 `'원시악어류'`를 절멸시키도록 되어 있으나, `buildEcosystem()`(L618~628)이 생성하는 8개체에 **'원시악어류'가 없습니다.** 우측 카드는 원시악어류를 대표 화석으로 소개하는데 캔버스에는 등장조차 하지 않습니다. → `buildEcosystem()`에 `new FossilCreature('원시악어류', 'meso')` 추가 필요.

3. **생존 기간 막대가 부정확합니다.**

   | 생물 | 코드 값 | 실제 | 문제 |
   |---|---|---|---|
   | 공룡 | 50%~90% | 트라이아스기 후기~백악기 말(=100%) | 백악기 말까지 못 감 |
   | 암모나이트 | 50%~95% | **데본기(고생대)**~백악기 말 | 시작이 중생대로 잘못됨 |
   | 필석 | 0%~25% | 캄브리아기~데본기 초 | 대략 타당 |

   막대의 축 레이블이 "고생대 초 / 고생대 말 / 중생대 말" 3점뿐이라 눈금 해상도도 부족합니다. 이 막대는 **표준화석의 '짧은 생존 기간' 조건을 가르칠 최적의 도구**인데 부정확해서 못 쓰고 있습니다.

4. **'외권'이라는 용어**(L548). 소행성 충돌을 `[외권]`으로 표기했는데, 지구시스템 5권역에서 '외권(exosphere)'은 **대기권 최상층**을 뜻합니다. 소행성은 지구시스템 **외부 요인**이므로 `[지구 외 요인]` 등으로 표기해야 합니다. 오히려 이 지점은 **"5대 멸종 중 지구시스템 외부에서 촉발된 것은 몇 번인가?"**라는 좋은 질문거리입니다(정답: 5차 하나뿐 — 나머지는 모두 지구 내부 요인).

5. **캔버스가 반응형이 아닙니다.** `resize()`(L631)로 캔버스 크기는 부모에 맞추지만, 4권역 노드 좌표는 `x: 90/280/470`로 **고정**되어 있습니다(L705~708). 넓은 모니터에서는 좌측에 몰리고, 폭 700px 이하에서는 수권 노드가 잘립니다. 생물 개체의 y 좌표(210~340)도 고정이라 세로가 짧으면 겹칩니다.

6. `alertSystem: "CAMP"`가 정의되어 있으나 `loop()`의 배경색 분기(L752~755)에 처리가 없어 4차 대멸종만 시각적 경고 연출이 빠집니다. `IMPACT`도 배경색 분기에는 없습니다.

7. 모든 생물 이모지가 `🏊`(수영)입니다(L611). 공룡과 원시악어류에도 적용되어 육상 생물이 헤엄치는 것으로 보입니다.

---

## 4. 활동 간 일관성 문제

같은 사이트인데 활동마다 규칙이 다릅니다. 학생은 활동을 옮길 때마다 인터페이스를 새로 배워야 합니다.

| 항목 | 불일치 현황 |
|---|---|
| **지층 표현 방향** | `exodus`는 위=최신(정확) / `geoperiod`는 깊을수록 최신(오류) — **같은 주제, 정반대 규칙** |
| **오답 피드백** | `exodus`만 오답별 해설 / 나머지는 빨간 테두리만 |
| **문항 형식** | 서술형(구름, 저기압, 지구시스템, 탄소순환) vs 객관식(핵융합, 별진화, 지질시대, 대멸종) — 기준 없음 |
| **하단 요약** | `footer-theory` 1줄(4종) vs `summary-section` 3카드(2종) vs 없음(2종) |
| **초기화 버튼** | 있음 4종 / 없음 4종 |
| **활동 제목** | "지구 시스템 16대 상호작용 매트릭스 고증 패드" 같은 조어가 다수 |

특히 마지막 항목 — **"고증", "매핑", "연산", "엔진", "밸런싱"** 같은 표현이 UI 문구와 주석에 광범위하게 쓰였습니다. 개발 과정의 흔적으로 보이나, 학생이 보는 화면에 남으면 과학 용어와 섞여 혼란을 줍니다. 예: "전선 미조우 상태일 때 화살표 회전 방향 벡터 매핑 교정"(주석), "다단계 지층 시추소 [피드백 2 반영]"(**화면에 노출됨**, `geoperiod.html` L369).

> `geoperiod.html` L369의 `[피드백 2 반영]`은 학생 화면에 그대로 보입니다. 개발 메모가 UI에 유출된 사례이니 전수 점검이 필요합니다.

---

## 5. 과학적 오류 총정리 (우선순위순)

| # | 파일 | 위치 | 문제 | 심각도 |
|---|---|---|---|:---:|
| 1 | geoperiod | L717–725 | 지층 누중 법칙 역전 (깊을수록 최신) | 🔴🔴 |
| 2 | nucleon_binding | L371–387, L494 | 중수소가 렌더링되지 않아 소멸로 보임 | 🔴🔴 |
| 3 | extratropical | L382–384, L606–608 | 저기압 북쪽 통과 시 풍향 3건 모두 오류 | 🔴 |
| 4 | geoperiod | L396–401 | 지질시대 상대 길이 균등 배치 | 🔴 |
| 5 | exodus | L771 | 완족류 절멸 표시 (실제 현생) | 🔴 |
| 6 | cloud_experiment | L461, L463 | 단열 온도 변화가 압력비 무시 + 이슬점 판정 없음 | 🔴 |
| 7 | extratropical | L365–374 vs L398–401 | 상태판·그래프 기온 불일치 | 🟠 |
| 8 | exodus | L618–628, L774 | 원시악어류 미생성 → 4차 멸종 연출 없음 | 🟠 |
| 9 | earthsystem | L305 | 태국어 `และ` 혼입 | 🟠 |
| 10 | evolution_star | L182 | 초신성 물리량 임의값 | 🟠 |
| 11 | exodus | L514, L570 | 공룡·암모나이트 생존 기간 막대 부정확 | 🟠 |
| 12 | carboncycle | 전역 | 탄소 저장량 수치 부재(Q2가 답 불가) | 🟠 |
| 13 | evolution_star | 전역 | H-R도 부재 | 🟠 |
| 14 | geoperiod | L484, L468 | "거충류" 오타, 완족류 대칭성 설명 오류 | 🟡 |
| 15 | exodus | L548 | '외권' 용어 오용 | 🟡 |
| 16 | carboncycle | L476 | 대기 중 파티클을 HCO₃⁻로 표기 | 🟡 |
| 17 | nucleon_binding | L475 | 임계온도 계단 함수 (터널링 미반영) | 🟡 |
| 18 | earthsystem | L310, L315 | 염류 기원(Cl⁻)·심층순환 요인 설명 불완전 | 🟡 |

---

## 6. 개선 로드맵

### 0순위 — 이번 주 (총 3~4시간, 수업 투입 전 필수)

오개념을 만드는 항목만 골랐습니다. 코드 수정량이 적고 효과가 즉각적입니다.

1. `index.html`의 `example.streamlit.app` 링크 5개 제거 또는 "준비 중" 비활성화 — **10분**
2. `geoperiod.html` 시추 순서 역전 수정 (신생대 100m → 중생대 500m → 고생대 1,500m → 선캄브리아 3,000m) — **30분**
3. `geoperiod.html` 하단 타임라인을 실제 기간 비율(88.2 : 6.3 : 4.0 : 1.4)로 `flex-grow` 배분 — **15분**
4. `nucleon_binding.html`에 `'neu'` 렌더링 분기 추가 — **10분**
5. `extratropical_cyclone.html` 북쪽 통과 풍향 3건 + 화살표 각도 수정 (§3-2에 코드 제공) — **30분**
6. `extratropical_cyclone.html` 기온 값을 단일 소스로 통합 — **20분**
7. `exodus.html` 완족류·필석을 절멸이 아닌 '급감' 표시로 변경 + `buildEcosystem()`에 원시악어류 추가 — **20분**
8. `earthsystem.html` 태국어 제거, `geoperiod.html` "[피드백 2 반영]"·"거충류" 등 개발 메모·오타 전수 정리 — **30분**

### 1순위 — 이번 달 (활동을 '탐구'로 전환)

9. **`carboncycle.html`에 저장소 규모(로그 스케일 막대) + 빠른/느린 순환 속도 차이 도입** — 가장 효과가 큽니다.
10. **`evolution_star.html`의 게이지 3개를 H-R도로 교체**, 초신성 물리량을 "측정 불가"로 정정, 중성자별 단계 추가.
11. **`earthsystem.html`을 '정답 열람'에서 '사례 분류'로 전환** — 빈 매트릭스 + 실제 사건 카드 배치 미션.
12. **모든 활동에 '답안 기록 + 내보내기' 모듈 추가** (§7-2). 이것이 없으면 정규 수업 평가에 쓸 수 없습니다.
13. **오답 해설을 `exodus.html` 패턴으로 전 활동에 통일.**
14. **반응형 대응**: 최소 1,024px에서 정상 동작하도록 사이드바 접기 + 캔버스 스케일링 (§8-2).

### 2순위 — 학기 중 (프로젝트 목표 정렬)

15. **`cloud_experiment.html`을 실물 실험 + 센서 계측 대시보드로 재설계** — 프로젝트 정체성 회복의 상징적 작업.
16. **`extratropical_cyclone.html`에 기상청 실황 데이터 연결** — 시뮬레이션에서 배운 패턴을 실데이터로 검증.
17. **비어 있는 '지구과학' 메뉴 채우기** — 지진파 주시곡선(마이크로비트 가속도 센서로 진동 측정), 태양 고도–조도 관계(마이크로비트 조도 센서), 지구자기장 측정(스마트폰 자기 센서). 모두 센서 기반 활동으로 설계 가능합니다.
18. `geoperiod.html` 게임 보상 구조 재설계 (무작위 발굴 → 지층 판정).
19. 접근성 (키보드 내비게이션, `aria-*`, 색상 대비 4.5:1).
20. 외부 자산 로컬화 (NASA GIF, Pretendard 폰트).

---

## 7. 구조 개편 제안

### 7-1. 모든 활동에 '탐구 3단 구조' 적용

현재 활동들의 공통 구조는 **[조작] → [읽기]** 2단입니다. 여기에 앞뒤를 붙이면 탐구가 됩니다.

```
[1] 예상  →  [2] 조작·관찰  →  [3] 설명·일반화
 조작 전에 예측을 입력      현재 구현된 부분      예측과 결과의 차이를 설명
 (틀려도 무방, 저장됨)                          + 실생활 사례로 전이
```

**[1] 예상 단계가 결정적입니다.** 활동 시작 시 "압력을 2배로 올린 뒤 뚜껑을 열면 온도는 몇 도가 될까?"를 **먼저 입력**하게 하고, 관찰 후 자신의 예측과 비교시키면 인지 갈등이 발생해 개념 변화가 일어납니다. 예측 없이 시뮬레이션만 보면 학생은 "그렇구나"하고 지나가며 기존 오개념이 그대로 남습니다.

구현 비용은 낮습니다 — 각 활동에 진입 모달 하나와 `<textarea>` 하나면 충분합니다.

### 7-2. 학습 기록 모듈 (전 활동 공통)

가장 시급하고, 가장 재사용성이 높은 개선입니다. 파일 하나를 만들어 모든 활동에서 `<script src="worksheet.js">` 한 줄로 불러 쓰는 구조를 권합니다.

**기능 요건**
- 활동 시작 시 학번·이름 입력 (1회, `localStorage` 유지)
- 서술형 문항을 `<textarea>`로 렌더링, 입력 즉시 자동 저장
- 객관식 응답·시도 횟수·정답 여부 기록
- 시뮬레이션 조작 로그 기록 (예: 저기압 활동에서 학생이 관측한 시각별 기온·기압 — **이것이 곧 학생의 관측 데이터**)
- **"제출용 파일 저장"** 버튼 → `학번_이름_활동명.txt`(또는 CSV) 다운로드
- 교사용: 여러 파일을 한 번에 읽어 표로 보여 주는 취합 페이지

**주의**: Claude 아티팩트 환경과 달리 학교 브라우저에서는 `localStorage`를 쓸 수 있으나, **공용 PC에서는 다음 학생 데이터와 섞입니다.** 활동 종료 시 자동 초기화 + 파일 다운로드 유도를 기본 흐름으로 설계하세요. 구글 클래스룸을 쓰신다면 다운로드 파일을 과제로 제출받는 방식이 가장 안정적입니다.

### 7-3. 센서 연동 아키텍처 (프로젝트 본령)

현재 사이트에는 센서 관련 코드가 전혀 없습니다. 도입 시 선택지는 셋입니다.

| 방식 | 대상 | 장점 | 제약 |
|---|---|---|---|
| **Web Bluetooth** | 마이크로비트 | 설치 불필요, 웹페이지에서 직접 실시간 수신 | **HTTPS 필수** (file:// 불가), Chrome/Edge만 |
| **CSV 업로드** | 마이크로비트 데이터 로거, phyphox | 가장 안정적, 기기·브라우저 무관 | 실시간 아님 |
| **Generic Sensor API** | 스마트폰 내장 센서 | 별도 기기 불필요 | HTTPS 필수, iOS 지원 제한적 |

> **권장 전략: CSV 업로드를 기본으로 하고, Web Bluetooth를 선택지로 얹기.**
>
> 이유는 **HTTPS 요구사항** 때문입니다. 현재 사이트는 로컬 파일(`file://`)로 열리는 구조인데, Web Bluetooth와 Generic Sensor API는 **보안 컨텍스트(HTTPS 또는 localhost)에서만 동작**합니다. 즉 센서를 웹에서 직접 읽으려면 **GitHub Pages 등에 배포하는 것이 선행 조건**입니다.
>
> GitHub Pages 배포는 무료이고 자동 HTTPS가 적용되며, 부수적으로 ① 학생이 URL만으로 접속(파일 배포 불필요) ② 수정 사항 즉시 반영 ③ 버전 관리라는 이점을 함께 얻습니다. **1순위 작업 목록에 넣을 가치가 충분합니다.**

수업 흐름은 이렇게 됩니다:
```
[교실] 마이크로비트로 측정 → CSV 저장
   ↓
[웹] 사이트에 CSV 업로드 → 자동 그래프 + 이론 곡선 중첩
   ↓
[분석] 실측과 이론의 차이를 설명 → 오차 요인 토의
```
마지막 단계가 핵심입니다. **실측과 이론이 어긋나는 지점이 가장 좋은 탐구 질문**이며, 순수 시뮬레이션으로는 결코 만들 수 없는 학습 기회입니다.

---

## 8. 즉시 적용 가능한 코드

### 8-1. 딥링크 라우팅 (`script.js` 교체)

학생에게 "이 주소 열어라"로 특정 활동을 바로 안내할 수 있게 됩니다.

```js
document.addEventListener('DOMContentLoaded', () => {
    const panels = {
        home: document.getElementById('home-panel'),
        info: document.getElementById('info-panel'),
        app:  document.getElementById('app-panel')
    };
    const frame = document.getElementById('content-frame');

    function show(which, url) {
        Object.values(panels).forEach(p => p.classList.remove('active'));
        panels[which].classList.add('active');
        if (which === 'app') {
            // 상대경로끼리 비교해야 불필요한 리로드를 막을 수 있음
            if (frame.getAttribute('src') !== url) frame.setAttribute('src', url);
        } else {
            frame.setAttribute('src', '');
        }
    }

    function route() {
        const hash = location.hash.slice(1);
        if (!hash || hash === 'home') return show('home');
        if (hash === 'info') return show('info');
        show('app', hash);
    }

    document.querySelectorAll('.menu-item').forEach(item => {
        item.addEventListener('click', () => {
            location.hash = item.dataset.url;   // 뒤로 가기 지원
        });
    });
    document.querySelector('.info-btn').addEventListener('click', () => location.hash = 'info');
    document.querySelector('.logo').addEventListener('click',    () => location.hash = 'home');

    window.addEventListener('hashchange', route);
    route();
});
```

### 8-2. 캔버스 반응형 스케일링 (전 활동 공통)

내부 좌표계를 그대로 두고 CSS로만 축소해, **좌표 계산 코드를 한 줄도 고치지 않고** 작은 화면에 대응합니다.

```css
/* 각 활동 <style>에 추가 */
canvas {
    max-width: 100%;
    height: auto;          /* 종횡비 유지 */
}
.sidebar-left, .sidebar-right {
    flex-shrink: 0;
}
@media (max-width: 1280px) {
    .sidebar-right { display: none; }         /* 고찰 문항은 별도 토글로 */
    body { overflow-y: auto; }
}
@media (max-width: 1024px) {
    .main-wrapper { flex-direction: column; height: auto; }
    .sidebar-left { width: 100%; }
    body { height: auto; overflow-y: auto; }
}
```

⚠️ **주의**: 캔버스를 CSS로 축소하면 마우스 좌표 변환이 어긋납니다. 클릭 판정이 있는 활동(`cloud_experiment`, `geoperiod`)은 아래 보정이 반드시 필요합니다.

```js
function getCanvasPos(e, canvas) {
    const rect = canvas.getBoundingClientRect();
    return {
        x: (e.clientX - rect.left) * (canvas.width  / rect.width),
        y: (e.clientY - rect.top)  * (canvas.height / rect.height)
    };
}
```

### 8-3. `splice`-in-`forEach` 일괄 수정

`earthsystem.html`(L648), `carboncycle.html`(L497), `nucleon_binding.html`(L647)에 공통 적용:

```js
// 잘못된 패턴 — 순회 중 삭제로 인덱스가 밀림
particles.forEach((p, idx) => { ...; particles.splice(idx, 1); });

// 수정 A: 역순 순회
for (let i = particles.length - 1; i >= 0; i--) {
    const p = particles[i];
    // ... 갱신 및 그리기
    if (shouldRemove) particles.splice(i, 1);
}

// 수정 B: 갱신·그리기와 제거를 분리 (가독성 우수)
particles.forEach(p => { updateAndDraw(p); });
particles = particles.filter(p => !p.dead);
```

---

## 9. 맺으며

이 사이트의 가장 큰 자산은 **8개 활동이 하나의 설계 철학을 공유한다**는 점입니다. 개별 페이지를 아무리 잘 만들어도 이 일관성은 나중에 만들 수 없는데, 이미 갖고 계십니다.

지금 필요한 것은 활동을 더 만드는 일이 아니라 **방향을 트는 일**입니다.

- **§6의 0순위 8개 항목**은 오개념을 만드는 것들이므로, 수업 투입 전에 반드시 정리하셔야 합니다. 총 3~4시간이면 끝납니다.
- **§7-2 학습 기록 모듈**은 이 사이트를 '보여 주는 자료'에서 '수업에 쓰는 도구'로 바꾸는 단 하나의 변곡점입니다.
- **§7-3 센서 연동**은 선생님 프로젝트의 본령입니다. GitHub Pages 배포가 그 전제 조건이라는 점만 기억하시면 됩니다.

우선순위를 하나만 꼽으라면 — **`carboncycle.html`에 저장량 수치를 넣는 것**입니다. 코드 변경량은 반나절이지만, 이미 잘 만든 중첩 UI가 정량 데이터를 만나 순식간에 최상위 탐구 활동이 됩니다. 그 경험이 나머지 7개를 고칠 기준점이 되어 줄 것입니다.

---

*본 보고서는 소스 코드 정적 분석에 기반합니다. 실제 브라우저 렌더링 검증(레이아웃 깨짐, 애니메이션 성능, 학교 기기 호환성)은 별도로 진행하시길 권합니다.*
